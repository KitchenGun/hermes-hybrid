# 시크릿 (절대 커밋 금지)
.env
auth.json
auth.json.bak
*.pem
*.key

# 런타임 상태 (WSL 로컬에 생성됨, 실수로 복사돼도 커밋 안 함)
memories/
sessions/
logs/
.cache/

# 운영체제
.DS_Store
Thumbs.db

# 단, 디렉토리 자체는 유지해야 하므로 gitkeep는 허용
!memories/.gitkeep
!sessions/.gitkeep
!logs/.gitkeep
