{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MailOpsIntent",
  "description": "Intent schema for the mail_ops profile. Read-only operations on Gmail and Naver mailboxes.",
  "type": "object",
  "required": ["action"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["list_inbox", "search", "summarize", "alert"],
      "description": "수행할 동작"
    },
    "account": {
      "type": "string",
      "description": "accounts.yaml에 정의된 계정 이름 (생략 시 전체)"
    },
    "query": {
      "type": "string",
      "description": "search action 전용 — 발신자 또는 제목 키워드"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 10
    }
  },
  "additionalProperties": false
}
