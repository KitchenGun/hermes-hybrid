<!-- Hermes processed memory: failure_patterns
     Canonical, human-editable. Items written by src/memory/ingestion/writer.py
     with stable item_id. Manual edits OK, but preserve item_id and meta block.
     Compiled into data/memory/MEMORY.md by MemoryCurator (Hermes Curator).
     Highest compile priority — failure_pattern items are surfaced first
     so Hermes avoids known traps.
     WARNING: Track only in private repos. -->

# Failure Patterns

Repeating failure modes Hermes has encountered: hard-coded assumptions,
fragile workflows, recurring user corrections. Captured so the agent
recognizes the pattern again rather than re-falling into it.

<!-- Items appended below this line as ## {title} sections with meta blocks. -->
