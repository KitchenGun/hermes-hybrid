<!-- Hermes processed memory: decision_log
     Canonical, human-editable. Items written by src/memory/ingestion/writer.py
     with stable item_id. Manual edits OK, but preserve item_id and meta block.
     Compiled into data/memory/MEMORY.md by MemoryCurator (Hermes Curator).
     WARNING: Track only in private repos.

     Scope: lifecycle / policy decisions, not code-level changes (those live
     in git log / git blame). Examples: tool selection, deprecation choices,
     stakeholder approvals, design trade-offs that span multiple PRs. -->

# Decision Log

Lifecycle and policy decisions with their rationale. Code-level decisions
belong in git log; this file is for cross-PR context.

<!-- Items appended below this line as ## {title} sections with meta blocks. -->
